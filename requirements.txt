# Versi dipaku sesuai lingkungan yang teruji, demi reprodusibilitas penelitian.

# Core numeric
numpy==1.26.4
numba>=0.59

# PyTorch (CPU – stabil untuk Python 3.10)
torch==2.0.1
torchaudio==2.0.2

# ASR
openai-whisper

# Speaker diarization
pyannote.audio==3.1.1
speechbrain>=0.5.14
torch-audiomentations==0.11.0

# PENTING - jangan naikkan ke 1.x. pyannote.audio 3.1.1 memanggil
# hf_hub_download(use_auth_token=...), dan parameter itu DIHAPUS di
# huggingface_hub 1.0. Dengan hub 1.x, diarization gagal total
# ("unexpected keyword argument 'use_auth_token'"). Batas ini pula yang
# mengunci versi gradio di bawah. Teruji: pyannote termuat dengan token
# sungguhan pada 0.36.2.
huggingface_hub==0.36.2

# UI. Versi 6.20.0 menarik huggingface_hub>=1.2.0 sehingga merusak pyannote
# (lihat catatan di atas); 6.17.3 adalah gradio terbaru yang masih menerima
# hub 0.x. Teruji: UI terbangun & ter-mount ke FastAPI.
gradio==6.17.3

# Web shell (FastAPI)
fastapi==0.139.0
uvicorn==0.51.0
jinja2==3.1.6
python-multipart==0.0.32

# Audio processing
pydub
noisereduce
soundfile

# Evaluator LLM
google-genai==2.12.0

# Basis data & keamanan
bcrypt==5.0.0

# Konfigurasi
python-dotenv==1.2.2
