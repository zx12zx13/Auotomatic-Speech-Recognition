# Versi dipaku sesuai lingkungan yang teruji, demi reprodusibilitas penelitian.

# Core numeric
numpy==1.26.4
numba>=0.59

# PyTorch (CPU – stabil untuk Python 3.10)
torch==2.0.1
torchaudio==2.0.2

# ASR
openai-whisper

# Speaker diarization
pyannote.audio==3.1.1
speechbrain>=0.5.14
torch-audiomentations==0.11.0
# CATATAN: di-upgrade dari 0.20.3 bersama gradio 6. Pembangunan UI sudah
# teruji; pemuatan pyannote dengan token sungguhan perlu diuji ulang
# begitu .env terisi.
huggingface_hub==1.23.0

# UI (di-upgrade dari 3.50.2; UI & mounting teruji pada 6.20.0)
gradio==6.20.0

# Web shell (FastAPI)
fastapi==0.139.0
uvicorn==0.51.0
jinja2==3.1.6
python-multipart==0.0.32

# Audio processing
pydub
noisereduce
soundfile

# Evaluator LLM
google-genai==2.12.0

# Basis data & keamanan
bcrypt==5.0.0

# Konfigurasi
python-dotenv==1.2.2
